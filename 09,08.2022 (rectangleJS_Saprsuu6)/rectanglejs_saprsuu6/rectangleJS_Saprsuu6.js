class Rectangle {
  constructor(height, width) {
    this.height = height;
    this.width = width;
  }

  GetHeight() {
    return this.height;
  }

  GetWidth() {
    return this.width;
  }
}

module.exports = Rectangle;
